import{formatDate,formatMoney}from"./utils.js";export function renderLineChart(rows,target,{money=false}={}){const w=600,h=180,p={l:42,r:10,t:12,b:24},max=Math.max(...rows.map(x=>x.value))*1.08,min=0,x=i=>p.l+i*(w-p.l-p.r)/(rows.length-1),y=v=>p.t+(max-v)*(h-p.t-p.b)/(max-min),line=rows.map((d,i)=>`${i?"L":"M"}${x(i)},${y(d.value)}`).join(" "),area=`${line} L${x(rows.length-1)},${h-p.b} L${x(0)},${h-p.b} Z`;target.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><defs><linearGradient id="areaFill" x1="0" y1="0" x2="0" y2="1"><stop stop-color="#356ec9" stop-opacity=".22"/><stop offset="1" stop-color="#356ec9" stop-opacity="0"/></linearGradient></defs>${Array.from({length:5},(_,i)=>{const yy=p.t+i*(h-p.t-p.b)/4;return`<line class="gridline" x1="${p.l}" x2="${w-p.r}" y1="${yy}" y2="${yy}"/><text class="axis-label" x="0" y="${yy+3}">${Math.round(max*(4-i)/4)}</text>`}).join("")}<path class="series-area" d="${area}"/><path class="series-line" d="${line}"/>${rows.map((d,i)=>`<g class="chart-hit" data-i="${i}"><rect x="${x(i)-7}" y="0" width="14" height="${h}" fill="transparent"/><circle class="chart-dot" cx="${x(i)}" cy="${y(d.value)}" r="3"/></g>`).join("")}${rows.filter((_,i)=>i%4===0).map((d,i)=>`<text class="axis-label" x="${x(i*4)}" y="${h-5}" text-anchor="middle">${formatDate(d.date)}</text>`).join("")}</svg><div class="tooltip" hidden></div>`;const tip=target.querySelector(".tooltip");target.querySelectorAll(".chart-hit").forEach(g=>{g.onmouseenter=e=>{const d=rows[+g.dataset.i];tip.hidden=false;tip.textContent=`${formatDate(d.date)} · ${money?formatMoney(d.value):d.value}`;tip.style.left=(e.currentTarget.getBoundingClientRect().left-target.getBoundingClientRect().left+7)+"px";tip.style.top="55px"};g.onmouseleave=()=>tip.hidden=true})}