import{formatMoney,pctDelta}from"./utils.js";export function renderKPIs(rows,target){target.innerHTML=rows.map(x=>{const d=pctDelta(x.delta);return`<button class="card kpi" data-kpi="${x.id}"><div class="kpi-label">${x.label}</div><div class="kpi-value">${x.format==="money"?formatMoney(x.value):x.value.toLocaleString()}</div><div class="delta ${d.className}">${d.text}</div></button>`}).join("");target.querySelectorAll("[data-kpi]").forEach(b=>b.onclick=()=>alert("Opening detailed report: "+b.dataset.kpi))}