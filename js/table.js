import{formatMoney}from"./utils.js";export function renderTable(rows,target,state){const sorted=[...rows].sort((a,b)=>(a[state.sort.key]>b[state.sort.key]?1:-1)*state.sort.dir);target.innerHTML=`<table><thead><tr><th data-sort="name">Product</th><th data-sort="revenue">Revenue</th><th data-sort="sold">Sold</th><th>Action</th></tr></thead><tbody>${sorted.map(x=>`<tr><td>${x.name}</td><td>${formatMoney(x.revenue)}</td><td>${x.sold}</td><td><button class="details" data-product="${x.name}">Details</button></td></tr>`).join("")}</tbody></table>`;target.querySelectorAll("[data-sort]").forEach(h=>h.onclick=()=>{state.sort={key:h.dataset.sort,dir:state.sort.key===h.dataset.sort?-state.sort.dir:-1};renderTable(rows,target,state)});target.querySelectorAll("[data-product]").forEach(b=>b.onclick=()=>alert("Product details: "+b.dataset.product))}