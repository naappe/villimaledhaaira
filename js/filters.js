export function initFilters(DATA,state,onChange){const outlet=document.querySelector("#outlet"),from=document.querySelector("#from"),to=document.querySelector("#to");outlet.innerHTML=DATA.meta.outlets.map(x=>`<option value="${x.id}">${x.name}</option>`).join("");from.value=DATA.meta.range.from;to.value=DATA.meta.range.to;document.querySelector("#filters").onsubmit=e=>{e.preventDefault();state.outletId=outlet.value;state.dateRange={from:from.value,to:to.value};onChange()}}