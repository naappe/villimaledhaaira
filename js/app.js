import{DATA}from'./data.js';import{initSidebar}from'./sidebar.js';import{initFilters}from'./filters.js';import{renderLineChart}from'./charts.js';import{renderKPIs}from'./kpi.js';import{renderTable}from'./table.js';import{initExpenses}from'./expenses.js';import{initWorkflow,workflowRoutes,getWorkflowTitle}from'./workflows.js';const state={sidebarCollapsed:false,drawerOpen:false,outletId:'all',dateRange:{...DATA.meta.range},sort:{key:'revenue',dir:-1}};const routes={dashboard:{file:'dashboard.html',title:'Sales Dashboard',kind:'dashboard'},expenses:{file:'expenses.html',title:'Expenses',kind:'expenses'}};workflowRoutes.forEach(name=>routes[name]={file:'workspace.html',title:getWorkflowTitle(name),kind:'workflow'});function filterSeries(rows){const{from,to}=state.dateRange;return rows.filter(({date})=>(!from||date>=from)&&(!to||date<=to))}function renderDashboard(){renderLineChart(filterSeries(DATA.series.netSales),document.querySelector('#net-sales-chart'),{money:true});renderLineChart(filterSeries(DATA.series.salesCount),document.querySelector('#sales-count-chart'));renderKPIs(DATA.kpis,document.querySelector('#kpi-grid'));renderTable(DATA.topProducts,document.querySelector('#products-table'),state)}function initBanner(){const b=document.querySelector('#mobile-banner');if(!b)return;if(localStorage.getItem('ewity-mobile-banner-dismissed')==='true')b.hidden=true;document.querySelector('#dismiss-banner')?.addEventListener('click',()=>{b.hidden=true;localStorage.setItem('ewity-mobile-banner-dismissed','true')})}async function navigate(routeName){const name=routes[routeName]?routeName:'dashboard',route=routes[name],response=await fetch(`./${route.file}?v=6`);if(!response.ok)throw new Error(`${route.title} could not be loaded (${response.status})`);document.querySelector('#content').innerHTML=await response.text();document.querySelector('#page-title').textContent=route.title;document.title=`Ewity POS · ${route.title}`;document.querySelectorAll('[data-route]').forEach(x=>x.classList.toggle('active',x.dataset.route===name));history.replaceState(null,'',`#${name}`);if(route.kind==='dashboard'){initFilters(DATA,state,renderDashboard);initBanner();renderDashboard()}else if(route.kind==='expenses')initExpenses();else initWorkflow(name);document.querySelector('.app').classList.remove('drawer-open');window.scrollTo(0,0)}async function bootstrap(){initSidebar(state);document.addEventListener('click',e=>{const link=e.target.closest('[data-route]');if(!link)return;e.preventDefault();navigate(link.dataset.route).catch(showError)});window.addEventListener('hashchange',()=>navigate(location.hash.slice(1)).catch(showError));await navigate(location.hash.slice(1)||'dashboard')}function showError(error){console.error(error);document.querySelector('#content').innerHTML=`<section class="card load-error"><h1>Page unavailable</h1><p>${error.message}</p></section>`}bootstrap().catch(showError);
